# 1단계(데이터 수집)는 Python 표준 라이브러리만으로 동작합니다.
# 아래는 이후 단계(저장/분석/출력)에서 사용할 예정인 의존성입니다.
# 지금 단계에서 반드시 설치할 필요는 없습니다.

# --- 저장: Firebase Firestore (REST API + 서비스계정 토큰) ---
google-auth>=2.0       # 서비스계정 OAuth2 토큰 발급
# REST 호출은 표준 urllib 사용 (이 환경의 gRPC 차단 우회).
# (firebase-admin 의 gRPC 가 막힌 환경 대비 — REST 방식 채택)

# --- 데이터 수집: 외부 API 호출 ---
httpx>=0.27            # (구) Supabase REST / 마이그레이션 스크립트 등에서 사용
                       # The Odds API 어댑터는 표준 urllib 사용

# --- 3단계 분석: LLM 감성분류 (예정) ---
# anthropic>=0.40        # 비정형 → 긍정/부정 신호 플래그

# --- 4단계 출력: 웹 대시보드 (예정) ---
# fastapi>=0.110
# uvicorn[standard]>=0.29
# jinja2>=3.1

# --- 실제 유료 데이터로 교체 시 (예정) ---
# (httpx 는 위에서 이미 설치됨 — The Odds API / 통계 API / 뉴스 API 호출에 재사용)
