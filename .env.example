# Supabase 설정 — 이 파일을 .env 로 복사해 실제 값을 채운다.
# .env 는 .gitignore 되므로 절대 커밋되지 않는다.

SUPABASE_URL=https://<your-project-ref>.supabase.co

# 클라이언트/읽기 전용(공개 전제, RLS로 보호). 선택.
SUPABASE_ANON_KEY=

# 서버 사이드 쓰기/적재용. RLS 우회 — 절대 클라이언트/레포에 노출 금지.
SUPABASE_SERVICE_ROLE_KEY=
